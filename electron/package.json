{
  "name": "blueprint",
  "version": "0.0.0",
  "main": "src/main.js",
  "author": "Ludopant Creative Studio",
  "description": "alpha version",
  "scripts": {
    "electron-builder": "electron-builder",
    "start": "electron-builder start",
    "build": "electron-builder build --win --publish never --dir",
    "release": "electron-builder build --win --publish always"
  },
  "private": true,
  "dependencies": {
    "electron-updater": "^4.3.8"
  },
  "devDependencies": {
    "electron": "^12.0.2",
    "electron-builder": "^22.10.5"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/marcgardent/ProtoBG.git"
  }
}
